# 联机肉鸽塔防小程序 — 技术文档（一）：架构与协议

> **文档版本**：v1.0  
> **前端语言**：JavaScript（ES2020+）  
> **后端语言**：Node.js  
> **对应需求**：需求文档 §1-§4（全量覆盖，含三轮审阅补充）  

---

## 1. 技术选型

### 1.1 总体技术栈

| 层级 | 技术选择 | 说明 |
|------|----------|------|
| **游戏引擎** | Cocos Creator 3.8+ | 成熟微信小游戏打包方案，JS/TS 双语言支持 |
| **渲染方式** | Canvas + WebGL | 20×15 网格+大量单位，WebGL 保证性能 |
| **前端语言** | **JavaScript（ES2020+）** | 动态类型灵活，配合 JSDoc 注释增强可读性 |
| **后端运行时** | Node.js 18 LTS | 事件驱动，WebSocket 天然高性能 |
| **后端框架** | 接入层: uWebSockets.js / 逻辑层: 自研轻量框架 | 高并发 WebSocket 支持 |
| **通信协议** | WebSocket + JSON（帧数据用 MsgPack 压缩） | 实时双向通信，JSON 可读易调试 |
| **联机同步** | 帧同步（Lockstep） + 服务端校验 | 确定性逻辑保证 |
| **数据库** | MongoDB + Redis | 用户数据持久化 + 实时状态缓存 |
| **消息队列** | Redis Stream | 房间服务间消息路由 |
| **资源管理** | 微信分包 + CDN | 控制主包 < 4MB |

### 1.2 为什么选 JavaScript（而非 TypeScript）

| 维度 | JavaScript 优势 |
|------|----------------|
| **微信兼容** | 微信小游戏对 JS 原生兼容，无需额外编译 |
| **包体** | 无类型定义文件，包体更轻（省 ~15%） |
| **迭代速度** | 动态类型，快速原型迭代，适合肉鸽类需求频繁调整 |
| **团队门槛** | 小团队易上手，减少类型体操开销 |
| **质量保障** | JSDoc 注释 + ESLint 严格规则 + 单元测试弥补类型检查缺失 |

### 1.3 JS 与 TS 的关键差异对照

| 维度 | TypeScript 版 | JavaScript 版（本文档） |
|------|-------------|----------------------|
| 类型检查 | 编译期 | 运行时 + JSDoc `@typedef` |
| 接口/枚举 | `interface` / `enum` 关键字 | JSDoc `@typedef` / `Object.freeze()` |
| 泛型 | `class ObjectPool<T>` | `class ObjectPool`（无类型参数，JSDoc `@template`） |
| 配置表 | 类型字面量约束 | 普通对象 + `Object.freeze()` |
| 私密性 | `private` 关键字 | 命名约定 `_methodName` |
| 确定性保证 | 编译期防止误用 | 编码规范 + 单元测试 + 服务端校验 |

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    客户端（微信小游戏 / JavaScript）                │
│                                                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │ 渲染层    │ │ 逻辑层    │ │ UI 层     │ │ 网络层            │  │
│  │ Canvas/  │ │ 战斗逻辑  │ │ 大厅/房间 │ │ WebSocket + JSON  │  │
│  │ WebGL    │ │ 帧驱动    │ │ HUD/结算  │ │ + MsgPack(帧数据) │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘  │
└──────────────────────┬──────────────────────────────────────────┘
                       │  WSS (WebSocket Secure)
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    接入层（Gateway / Node.js）                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────────────────────┐ │
│  │ 连接管理  │ │ 消息路由  │ │ 负载均衡 / 房间分配 / 鉴权       │ │
│  │ 心跳检测  │ │ 协议转换  │ │ 限流 / 防作弊                    │ │
│  └──────────┘ └──────────┘ └──────────────────────────────────┘ │
└──────────────────────┬──────────────────────────────────────────┘
                       │  Redis Stream
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│               游戏逻辑层（Game Server / Node.js）                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │ 房间管理  │ │ 帧同步    │ │ 战斗逻辑  │ │ 匹配系统          │  │
│  │          │ │ Lockstep │ │ 服务端校验│ │ ELO/AI填充        │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘  │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                         数据层                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────────────────────┐ │
│  │ MongoDB  │ │ Redis    │ │ 微信云开发（可选：云函数/云存储）  │ │
│  │ 用户/成就 │ │ 会话/房间 │ │ 静态资源/CDN                     │ │
│  │ 天赋/排行 │ │ 匹配队列 │ │                                  │ │
│  └──────────┘ └──────────┘ └──────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 客户端目录结构（Cocos Creator + JavaScript）

```
assets/
├── scenes/                          # 场景文件
│   ├── MainMenu.scene               # 主菜单/大厅
│   ├── Room.scene                   # 房间等待
│   ├── Battle.scene                 # 战斗主场景
│   └── Tutorial.scene               # 新手引导关
│
├── scripts/
│   ├── core/                        # 核心框架
│   │   ├── GameRoot.js              # 游戏入口，初始化所有Manager
│   │   ├── EventBus.js              # 全局事件总线（发布/订阅）
│   │   ├── TimeManager.js           # 时间管理（逻辑帧/渲染帧）
│   │   ├── ResourceManager.js       # 资源加载管理（分包预加载）
│   │   ├── AudioManager.js          # 音效/BGM 管理
│   │   ├── ConfigManager.js         # 热更新配置表加载
│   │   └── PerformanceMonitor.js    # 性能监控（FPS/内存/帧耗时）
│   │
│   ├── network/                     # 网络层
│   │   ├── WebSocketClient.js       # WebSocket 封装（自动重连/心跳）
│   │   ├── MessageProtocol.js       # 消息类型常量 + 编解码
│   │   ├── FrameSyncManager.js      # 帧同步管理（发送/追帧/插值）
│   │   ├── ReconnectHandler.js      # 断线重连（含追帧逻辑）
│   │   └── PingMonitor.js           # 延迟监控（Green/Yellow/Red）
│   │
│   ├── battle/                      # 战斗模块
│   │   ├── BattleManager.js         # 战斗总控（状态机：PREPARE→FIGHTING→WAVE_END→FINISHED）
│   │   ├── BattleLogic.js           # 核心战斗逻辑（确定性，所有计算用定点数）
│   │   ├── EntityManager.js         # 实体管理（怪物/塔/子弹/小兵/特效 生命周期）
│   │   ├── TowerController.js       # 防御塔控制器（建造/升级/出售/目标切换）
│   │   ├── TowerAI.js               # 塔攻击AI（5种目标优先级）
│   │   ├── EnemyController.js       # 怪物控制器（生成/移动/特殊行为）
│   │   ├── BulletController.js      # 子弹/弹道管理
│   │   ├── WaveController.js        # 波次管理（生成节奏/时间轴）
│   │   ├── WaveEventQueue.js        # 波次间事件队列（成长→商店→骰子→抽卡→三选一）
│   │   ├── BuffManager.js           # Buff/Debuff 运行时管理
│   │   ├── EconomyManager.js        # 经济系统（金币/消耗/赠送）
│   │   ├── DamageCalculator.js      # 伤害计算（物/魔/真+护甲公式）
│   │   ├── CrystalController.js     # 水晶/萝卜 生命值+护盾
│   │   ├── ShopController.js        # 波次间商店系统
│   │   ├── PauseController.js       # 暂停/继续（单人+多人投票）
│   │   └── AutoTowerAI.js           # AI托管（断线/切后台/AI队友）
│   │
│   ├── map/                         # 地图模块
│   │   ├── MapGenerator.js          # 随机地图生成（BSP + A*）
│   │   ├── AStarPathfinding.js      # A* 寻路算法
│   │   ├── TileMapRenderer.js       # 瓦片地图渲染
│   │   ├── ObstacleManager.js       # 障碍物管理（装饰物）
│   │   └── HeatmapRenderer.js       # 怪物路径热力图（建造预览时显示）
│   │
│   ├── roguelike/                   # 肉鸽模块
│   │   ├── DiceSystem.js            # 骰子系统（服务端随机 → 客户端动画）
│   │   ├── DicePoolConfig.js        # 骰子四池配置（己方增益/敌方减益/怪物增益/队友减益）
│   │   ├── GachaSystem.js           # 抽卡系统（含独立保底计数器）
│   │   ├── GachaPoolConfig.js       # 抽卡池配置（N/R/SR/SSR）
│   │   ├── TowerGrowthSystem.js     # 塔属性成长系统（8种塔不同曲线）
│   │   ├── TowerPickSystem.js       # 塔三选一系统
│   │   ├── RelicSystem.js           # 遗物系统（10个SR/SSR遗物）
│   │   ├── RandomEventSystem.js     # 随机事件系统（15%触发率）
│   │   └── RouteBranchSystem.js     # 多路线分支系统（7/14波触发）
│   │
│   ├── social/                      # 社交/账号模块
│   │   ├── AuthManager.js           # 微信授权登录
│   │   ├── FriendManager.js         # 好友列表/在线状态
│   │   ├── RoomManager.js           # 房间管理（创建/加入/退出/踢人）
│   │   ├── MatchmakingManager.js    # 快速匹配（含AI填充）
│   │   ├── ChatManager.js           # 聊天（快捷消息+自由文本）
│   │   ├── VoiceManager.js          # 联机语音（wx.joinVoIPChat）
│   │   ├── SpectatorManager.js      # 观战系统（延迟10-30s）
│   │   └── ShareManager.js          # 微信分享（邀请/战报/成就）
│   │
│   ├── ui/                          # UI 模块
│   │   ├── MainMenuUI.js            # 主菜单/大厅
│   │   ├── RoomUI.js                # 房间界面
│   │   ├── BattleHUD.js             # 战斗 HUD（波次/金币/生命/道具栏）
│   │   ├── TowerSelectUI.js         # 塔选择面板（拖拽建造）
│   │   ├── TowerInfoUI.js           # 塔详情面板（含目标模式切换）
│   │   ├── TowerRangeUI.js          # 塔射程指示圈
│   │   ├── DiceRollUI.js            # 骰子动画展示（普通/快速/跳过）
│   │   ├── DiceResultUI.js          # 骰子结果展示（三张效果卡片）
│   │   ├── GachaDrawUI.js           # 抽卡展示界面（含保底进度条）
│   │   ├── BuffSelectUI.js          # 塔三选一/Buff选择面板
│   │   ├── ShopUI.js                # 波次间商店界面
│   │   ├── SettlementUI.js          # 结算界面（含评分S/A/B/C/D+战报分享）
│   │   ├── TalentTreeUI.js          # 天赋树界面（三大分支树状图）
│   │   ├── AchievementUI.js         # 成就列表
│   │   ├── LeaderboardUI.js         # 排行榜
│   │   ├── CollectionUI.js          # 图鉴系统
│   │   ├── BattlePassUI.js          # 通行证
│   │   ├── ShopMallUI.js            # 皮肤商城
│   │   ├── SettingsUI.js            # 设置页（音频/隐私/帮助/服务通知）
│   │   ├── FeedbackUI.js            # 用户反馈/客服
│   │   ├── GuideMaskUI.js           # 新手引导遮罩+高亮
│   │   ├── DamagePopupUI.js         # 伤害飘字
│   │   ├── ToastUI.js               # 轻量提示气泡
│   │   └── LoadingUI.js             # 加载态/骨架屏
│   │
│   ├── entity/                      # 游戏实体（MVC 中的 Model）
│   │   ├── Tower.js                 # 防御塔基类
│   │   │   ├── ArrowTower.js        # 箭塔（物理单体）
│   │   │   ├── CannonTower.js       # 炮塔（范围溅射）
│   │   │   ├── IceTower.js          # 冰塔（减速控制）
│   │   │   ├── MagicTower.js        # 魔法塔（穿透法伤+Lv2反隐）
│   │   │   ├── PoisonTower.js       # 毒塔（DOT叠加）
│   │   │   ├── TeslaTower.js        # 电塔（连锁闪电+Lv3反隐）
│   │   │   ├── TotemTower.js        # 图腾塔（光环支援，自身不攻击）
│   │   │   └── SummonTower.js       # 召唤塔（召唤小兵阻挡）
│   │   ├── Enemy.js                 # 怪物基类
│   │   │   ├── NormalEnemy.js       # 普通小怪
│   │   │   ├── FastEnemy.js         # 疾行怪
│   │   │   ├── TankEnemy.js         # 重甲怪
│   │   │   ├── FlyingEnemy.js       # 飞行怪（需对空）
│   │   │   ├── HealerEnemy.js       # 治疗师
│   │   │   ├── SplitterEnemy.js     # 分裂怪
│   │   │   ├── StealthEnemy.js      # 隐行怪（需反隐）
│   │   │   ├── ShieldEnemy.js       # 护盾怪
│   │   │   ├── SummonerEnemy.js     # 召唤师
│   │   │   ├── BomberEnemy.js       # 自爆怪
│   │   │   ├── EliteEnemy.js        # 精英怪
│   │   │   └── BossEnemy.js         # BOSS（含4个BOSS子类）
│   │   ├── Bullet.js                # 子弹/弹道
│   │   ├── Minion.js                # 召唤塔小兵
│   │   ├── Crystal.js               # 水晶/萝卜
│   │   └── Effect.js                # 特效实体（攻击/死亡/爆炸/DOT）
│   │
│   ├── config/                      # 配置表（Object.freeze 冻结）
│   │   ├── TowerConfig.js           # 8种塔 × 3级 完整属性 + 费用
│   │   ├── EnemyConfig.js           # 12种怪物 + 4 BOSS 完整属性
│   │   ├── WaveConfig.js            # 波次配置（20波怪物组成 + 生成节奏）
│   │   ├── DicePoolConfig.js        # 骰子四池配置（34个效果）
│   │   ├── GachaPoolConfig.js       # 抽卡池配置（N/R/SR/SSR 共18种）
│   │   ├── BuffConfig.js            # Buff/Debuff 运行时配置
│   │   ├── RelicConfig.js           # 遗物配置（5 SR + 5 SSR）
│   │   ├── ShopConfig.js            # 商店商品库配置
│   │   ├── TalentConfig.js          # 天赋树配置
│   │   ├── AchievementConfig.js     # 成就配置
│   │   ├── DifficultyConfig.js      # 难度参数配置（简单/中等/困难）
│   │   ├── MultiplayerConfig.js     # 多人难度系数配置
│   │   ↓ MapConfig.js              # 地图生成参数配置
│   │   ├── GuideStepConfig.js       # 新手引导步骤配置
│   │   └── FTUEConfig.js            # 首次用户体验漏斗配置
│   │
│   ├── data/                        # 数据管理（持久化/缓存）
│   │   ├── UserDataManager.js       # 用户数据（等级/经验/金币/钻石）
│   │   ├── TalentDataManager.js     # 天赋数据（三大分支21个天赋点）
│   │   ├── AchievementDataManager.js# 成就进度+解锁状态
│   │   ├── CollectionDataManager.js # 图鉴收集进度
│   │   ├── SettingsDataManager.js   # 设置数据（音量/画质/隐私/服务通知授权）
│   │   ├── GuideDataManager.js      # 引导完成状态（阶段/触发式提示）
│   │   └── SeasonDataManager.js     # 赛季/通行证数据
│   │
│   └── utils/                       # 工具类
│       ├── FixedPoint.js            # 定点数（精度 0.001，跨平台确定性）
│       ├── SeededRandom.js          # 确定性随机数（Mulberry32）
│       ├── ObjectPool.js            # 通用对象池
│       ├── MathUtils.js             # 数学工具
│       ├── CryptoUtils.js           # 加密/哈希工具（MD5/状态哈希）
│       ├── TimeUtils.js             # 时间工具
│       ├── ScreenAdapter.js         # 屏幕适配（刘海/折叠屏/安全区）
│       └── Logger.js                # 日志工具（分级+埋点上报）
│
└── resources/                       # 静态资源
    ├── prefabs/                     # 预制体
    │   ├── towers/                  # 8种塔预制体
    │   ├── enemies/                 # 12种怪物预制体
    │   ├── bullets/                 # 子弹预制体
    │   └── effects/                 # 特效预制体
    ├── textures/                    # 贴图
    │   ├── tiles/                   # 瓦片贴图（5种类型）
    │   ├── ui/                      # UI贴图
    │   └── decorations/             # 装饰贴图（树木/岩石/花草）
    ├── audio/                       # 音频
    │   ├── bgm/                     # 背景音乐
    │   ├── sfx/                     # 音效
    │   └── voice/                   # 语音（可选）
    └── animations/                  # 动画文件
```

### 2.3 后端目录结构（Node.js）

```
server/
├── gateway/                         # 接入层（Gateway）
│   ├── index.js                     # 入口
│   ├── ConnectionManager.js         # WebSocket 连接管理
│   ├── MessageRouter.js             # 消息路由
│   ├── AuthMiddleware.js            # 鉴权中间件（token校验）
│   ├── RateLimiter.js               # 限流
│   └── LoadBalancer.js              # 负载均衡
│
├── game-server/                     # 游戏逻辑层
│   ├── index.js                     # 入口
│   ├── RoomManager.js               # 房间管理（创建/加入/退出/状态机）
│   ├── FrameSyncEngine.js           # 帧同步引擎（Lockstep）
│   ├── MatchmakingEngine.js         # 匹配引擎（含AI填充）
│   ├── BattleValidator.js           # 战斗逻辑校验（服务端权威校验）
│   ├── DiceService.js               # 骰子服务（服务端真随机+风控）
│   ├── GachaService.js              # 抽卡服务（保底计算）
│   ├── EconomyValidator.js          # 经济校验（防金币溢出/负数）
│   ├── SpectatorService.js          # 观战服务（延迟转发）
│   ├── ReconnectService.js          # 断线重连服务
│   ├── AutoTowerAI.js               # AI托管逻辑
│   └── AntiCheatService.js          # 反作弊（异常检测/风控）
│
├── data-service/                    # 数据服务
│   ├── UserService.js               # 用户CRUD
│   ├── TalentService.js             # 天赋树
│   ├── AchievementService.js        # 成就
│   ├── LeaderboardService.js        # 排行榜
│   ├── SeasonService.js             # 赛季通行证
│   └── AnalysisService.js           # 埋点数据收集
│
├── shared/                          # 共享模块
│   ├── FixedPoint.js                # 定点数（服务端同样用）
│   ├── SeededRandom.js              # 确定性随机
│   ├── DamageCalculator.js          # 伤害计算（客户端/服务端双份一致）
│   ├── MessageTypes.js              # 消息类型常量
│   └── ConfigTables/                # 配置表（JSON，热更新）
│
└── models/                          # 数据模型
    ├── User.js                      # 用户模型
    ├── Room.js                      # 房间模型
    ├── MatchRecord.js               # 对局记录
    └── LogEntry.js                  # 日志/埋点
```

---

## 3. 通信协议设计

### 3.1 消息总览

| 消息分类 | 消息类型 | 方向 | 说明 |
|---------|---------|:---:|------|
| **账号** | `auth_login` / `auth_rsp` | C→S / S→C | 微信授权登录 |
| **房间** | `create_room` / `join_room` / `leave_room` / `kick_player` | C→S | 房间操作 |
| **房间** | `room_update` | S→C | 房间状态广播 |
| **匹配** | `start_match` / `cancel_match` / `match_rsp` | C→S / S→C | 快速匹配 |
| **准备** | `player_ready` / `all_ready` | C→S / S→C | 准备状态 |
| **战斗** | `battle_start` / `frame_input` / `frame_broadcast` | S→C / C→S / S→C | 帧同步 |
| **波次** | `wave_start` / `wave_end` / `wave_events` | S→C | 波次生命周期 |
| **骰子** | `dice_result` | S→C | 骰子结果（服务端下发） |
| **抽卡** | `gacha_result` | S→C | 抽卡结果（服务端下发） |
| **商店** | `shop_open` / `shop_buy` / `shop_refresh` | S→C / C→S | 波次间商店 |
| **暂停** | `pause_request` / `pause_vote` / `pause_result` | C→S / S→C | 暂停投票 |
| **聊天** | `chat_message` | C→S→C | 聊天消息 |
| **语音** | 微信 VoIP API | C→C | 实时语音 |
| **观战** | `spectate_join` / `spectate_frame` / `spectate_leave` | C→S / S→C | 观战 |
| **结算** | `game_over` | S→C | 结算数据 |
| **断线** | `player_disconnect` / `reconnect` / `host_transfer` | S→C / C→S / S→C | 断线重连 |
| **社交** | `invite_friend` / `gift_gold` / `share_battle_report` | C→S→C | 社交互动 |

### 3.2 消息格式规范

```javascript
/**
 * 所有消息的通用信封格式
 * @typedef {object} Message
 * @property {string} type       - 消息类型
 * @property {string} [roomId]   - 房间号（房间/战斗消息必填）
 * @property {number} [seq]      - 消息序列号（用于去重和排序）
 * @property {number} timestamp  - 服务端时间戳
 * @property {*} data            - 消息体
 */
```

### 3.3 帧同步协议

```javascript
/**
 * 帧输入（客户端→服务端）
 * @typedef {object} FrameInput
 * @property {string} type          - "frame_input"
 * @property {string} roomId
 * @property {number} frameId       - 当前帧号
 * @property {Action[]} actions     - 本帧玩家的操作列表
 */

/**
 * 操作指令
 * @typedef {object} Action
 * @property {'PLACE_TOWER'|'UPGRADE_TOWER'|'SELL_TOWER'|'USE_ITEM'|
 *            'SELECT_TOWER_PICK'|'SWITCH_TARGET_MODE'|'GIFT_GOLD'|
 *            'PING_MAP'|'EMPTY'} type
 * @property {string} [towerType]   - 塔类型（建造时）
 * @property {string} [towerId]     - 塔ID（升级/出售/模式切换时）
 * @property {number} [gridX]       - 格子X坐标
 * @property {number} [gridY]       - 格子Y坐标
 * @property {string} [itemId]      - 道具ID
 * @property {string} [targetMode]  - 目标模式（FIRST/LAST/STRONG/WEAK/CLOSE）
 * @property {number} [goldAmount]  - 赠送金币数量
 * @property {string} [targetPlayerId] - 目标队友ID
 */

/** @enum {string} */
const ActionType = Object.freeze({
    PLACE_TOWER: 'PLACE_TOWER',
    UPGRADE_TOWER: 'UPGRADE_TOWER',
    SELL_TOWER: 'SELL_TOWER',
    USE_ITEM: 'USE_ITEM',
    SELECT_TOWER_PICK: 'SELECT_TOWER_PICK',
    SWITCH_TARGET_MODE: 'SWITCH_TARGET_MODE',
    GIFT_GOLD: 'GIFT_GOLD',
    PING_MAP: 'PING_MAP',
    EMPTY: 'EMPTY',
});

/**
 * 帧广播（服务端→所有客户端）
 * @typedef {object} FrameBroadcast
 * @property {string} type          - "frame_broadcast"
 * @property {number} frameId
 * @property {Object<string, Action[]>} inputs  - { playerId: actions[] }
 * @property {number} serverTime    - 服务端时间（对时用）
 * @property {string} [stateHash]   - 每3波的状态哈希（一致性校验）
 */
```

### 3.4 同步参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 逻辑帧率 | 15 fps | 每帧约 66.7ms |
| 渲染帧率 | 60 fps | 逻辑帧间插值 4 个渲染帧 |
| 帧超时 | 200ms | 超时则空操作前进 |
| 乐观帧 | 3 帧 | 客户端预执行，减少体感延迟 |
| 追帧缓冲区 | 60 帧 | 重连时追帧 |
| 操作缓冲 | 10 帧 | 网络抖动缓冲 |
| 状态哈希校验 | 每 3 波 | 不一致触发追帧 |
| 输入去重 | 基于 seq | 同一操作不重复执行 |

### 3.5 房间状态机

```
                    ┌─────────────┐
        创建房间 →  │   WAITING   │ ← 加入房间
                    │  等待玩家    │
                    └──────┬──────┘
                           │ 全员准备 + 房主开始 / 自动倒计时10s
                           ▼
                    ┌─────────────┐
                    │   READY     │
                    │  准备开始    │
                    └──────┬──────┘
                           │ 地图生成 + 下发种子
                           ▼
                    ┌─────────────┐
                    │  PLAYING    │ ← 断线重连可回到此状态
                    │  对局中     │
                    └──────┬──────┘
                           │ 水晶生命归零 / 通关20波
                           ▼
                    ┌─────────────┐
                    │  FINISHED   │
                    │  已结束     │
                    └─────────────┘
```

### 3.6 断线重连时序

```
客户端断线
    │
    ├── 5秒无心跳 → 服务端判定断线 ↓
    │
    ├── 服务端：游戏暂停10秒等待重连
    │     └── 超时 → AI托管接管（自动升级，不建新塔）
    │
    ├── 客户端重新建立 WebSocket 连接
    │     └── 发送 reconnect { roomId, playerId, token }
    │
    ├── 服务端下发追帧数据
    │     └── { currentFrame, frameHistory[60], gameState }
    │
    └── 客户端快速追帧 → 显示"同步中..." → 追帧完成恢复
          │
          ├── 120秒内 → 重连成功 ✅
          └── 超过120秒 → 视为放弃 ❌
                └── 该玩家的塔被移除，金币平均分给队友
```

### 3.7 房主转移协议

```
房主断线
    │
    ├── 房主断线 > 5秒 → 触发房主转移
    │     ├── 候选1：在线时间最长的队友
    │     └── 候选2：段位最高的队友（在线时长相同）
    │
    ├── 服务端广播 host_transfer { newHostId, newHostName }
    │
    ├── 原房主重连 → 不会自动恢复房主身份
    │     └── 新房主可手动转让
    │
    └── 仅剩1人 → 自动转为AI托管队友，单人继续
```

---

## 4. 消息详细定义

### 4.1 房间类

```javascript
// === 创建房间 ===
// C→S
const createRoomReq = {
    type: 'create_room',
    data: {
        difficulty: 2,          // 1=简单 2=中等 3=困难
        maxPlayers: 4,          // 2-4
        mapSeed: 1234567890,    // 可选，不传则随机
        enableVoice: false,     // 是否开启语音
    },
};

// S→C
const createRoomRsp = {
    type: 'create_room_rsp',
    data: {
        roomId: 'A1B2C3',       // 6位房间号
        difficulty: 2,
        maxPlayers: 4,
        mapSeed: 1234567890,
        hostId: 'player_001',
        players: [{
            id: 'player_001',
            nickName: '玩家A',
            avatarUrl: 'https://...',
            isReady: false,
            isHost: true,
        }],
    },
};

// === 房间状态更新（广播给房间内所有人）===
// S→C（广播）
const roomUpdate = {
    type: 'room_update',
    data: {
        roomId: 'A1B2C3',
        players: [
            { id: 'player_001', nickName: '玩家A', isReady: true, isHost: true, isOnline: true },
            { id: 'player_002', nickName: '玩家B', isReady: false, isHost: false, isOnline: true },
        ],
        status: 'WAITING',      // WAITING | READY | PLAYING | FINISHED
        countdown: 10,          // 全员准备后的倒计时，0表示未开始倒计时
    },
};
```

### 4.2 战斗类

```javascript
// === 波次结束 ===
// S→C（广播）
const waveEnd = {
    type: 'wave_end',
    data: {
        waveNumber: 3,
        players: [{
            id: 'player_001',
            goldEarned: 250,
            diceResult: {
                diceValue: 2,       // 1-6
                pool: 'PLAYER_FAVOR',
                buffs: [
                    { id: 'B-01', name: '攻击强化', poolType: 'PLAYER_BUFF', duration: 'PERMANENT' },
                    { id: 'D-03', name: '破甲诅咒', poolType: 'ENEMY_DEBUFF', duration: 'ONE_WAVE' },
                    { id: 'B-07', name: '生命恢复', poolType: 'PLAYER_BUFF', duration: 'INSTANT' },
                ],
            },
            gachaResult: {          // 每3波
                rarity: 'R',
                item: { id: 'R-02', name: '塔强化券' },
            },
            towerGrowth: {
                arrows: [{ towerId: 't_001', attack: '+3', attackSpeed: '-50ms' }],
                cannons: [{ towerId: 't_002', attack: '+5' }],
            },
            shopItems: [            // 特定波次
                { id: 'shop_001', type: 'ITEM', name: '冰冻炸弹', price: 120 },
                { id: 'shop_002', type: 'GOLD_PACK', name: '+100金币', price: 100 },
            ],
            towerPickOptions: [     // 特定波次，塔三选一
                { type: 'poison', name: '毒塔' },
                { type: 'tesla', name: '电塔' },
                { type: 'magic', name: '魔法塔' },
            ],
        }],
        bossWaveIncoming: true,     // 下波是否为BOSS波
        routeChoice: null,          // 路线选择（7/14波）
    },
};

// === 游戏结束 ===
// S→C
const gameOver = {
    type: 'game_over',
    data: {
        result: 'VICTORY',          // VICTORY | DEFEAT
        players: [{
            id: 'player_001',
            score: 92,              // S/A/B/C/D
            scoreGrade: 'A',
            wavesSurvived: 20,
            totalKills: 145,
            totalGoldEarned: 3200,
            remainingGold: 480,
            mvpTower: { type: 'arrow', kills: 58 },
            expGained: 450,
            goldBonus: 100,
        }],
        statistics: {
            totalWaves: 20,
            totalEnemies: 180,
            crystalFinalHp: 2,
            bossDefeated: [true, true, true, true],
        },
        shareCardUrl: 'https://cdn.xxx.com/share/card_a1b2c3.png',
    },
};
```

### 4.3 暂停/投票类

```javascript
// === 房主暂停 ===
// C→S
const pauseReq = { type: 'pause_request', data: { reason: 'host_pause' } };

// === 队员请求暂停（投票）===
// C→S
const pauseVote = { type: 'pause_vote', data: { action: 'request' } };

// S→C（广播投票状态）
const pauseVoteUpdate = {
    type: 'pause_vote_update',
    data: {
        initiatorId: 'player_002',
        votes: { 'player_001': 'yes', 'player_002': 'yes', 'player_003': null },
        requiredMajority: 0.5,
    },
};

// S→C（投票结果）
const pauseResult = {
    type: 'pause_result',
    data: { paused: true, reason: 'vote_passed', remainingPauses: 2 },
};
```

---

## 5. 资源分包方案

> 📌 根据需求 §16.6 美术资源量估算，战斗资源总量约6.8MB。需特殊拆分。

微信小程序主包限制 4MB，单个分包最大 4MB，小游戏代码包总上限 20MB。

**资源量估算**（来自需求 §16.6）：

| 资源类型 | 数量 | 预估单文件 | 预估总量 |
|---------|:---:|:-------:|:------:|
| 塔模型（含3级） | 8×3=24个 | 50-80KB | ~1.5MB |
| 怪物模型（含BOSS） | 12+4=16个 | 40-60KB | ~0.8MB |
| 塔贴图 | 24×2变体 | 30-50KB | ~1.8MB |
| 怪物贴图 | 16个 | 30-50KB | ~0.7MB |
| 音效（塔攻击+怪物+BGM） | 约40个 | 20-100KB | ~2.0MB |
| **战斗资源总计** | | | **~6.8MB** |

> ⚠️ 战斗资源 6.8MB > 4MB 单个分包限制 → 需拆分为 3 个分包。

```
主包（< 4MB）
├── 核心框架代码（core/network/utils）
├── 大厅/房间 UI 资源
├── 战斗 HUD 基础 UI
└── 分包加载控制器

分包1：战斗模型资源（< 4MB，进入房间时预加载）
├── 8种塔的模型（24个）               ~1.5MB
├── 12种怪物的模型+4 BOSS（16个）      ~0.8MB
├── 子弹/弹道基类模型                  ~0.2MB
└── 地图瓦片贴图                       ~0.5MB
    合计                               ~3.0MB

分包2：战斗贴图+特效（< 4MB，对局开始时加载）
├── 塔贴图（48张变体）                 ~1.8MB
├── 怪物贴图（16张）                   ~0.7MB
├── 爆炸/弹道/BOSS特效                 ~1.0MB
└── 地图装饰资源（树木/岩石/花草）      ~0.3MB
    合计                               ~3.8MB

分包3：音效+UI资源（< 4MB，按需加载）
├── 塔攻击/怪物/BGM音效（40个）        ~2.0MB
├── 骰子/抽卡/商店 UI 资源             ~0.5MB
├── 结算/评分动画                      ~0.4MB
├── 商城皮肤/主题资源                   ~0.5MB
└── 成就/排行榜/天赋树 UI 资源          ~0.3MB
    合计                               ~3.7MB

CDN 动态下发（不占包体）
├── 高清贴图（按设备分辨率动态选择）
├── 多语言文本（预留）
├── BGM 音频文件（流式加载）
├── 运营活动资源（活动期临时加载）
└── 图鉴大图（按需请求）
```

**分包预加载时机**：
- 进入房间后 → 立即开始预加载分包1（战斗模型）
- 全员准备后 → 加载分包2（贴图+特效）
- 分包3 按需加载：
  - 音效：首波开始前预加载核心音效（塔攻击+怪物死亡，~0.5MB），其余流式加载
  - UI资源：首次打开对应界面时加载

**资源版本管理**：
- 文件名含 hash：`arrow_tower_v3.a1b2c3d.png`
- 配置表热更新独立于客户端版本
- 资源降级策略：加载失败使用低精度占位资源
- CDN 资源通过 `wx.downloadFile` 下载后本地缓存

---

## 6. v1.0 开发范围 `🆕`

> 📌 来自需求 §8.1.1：经第四轮审阅确认的 v1.0 核心功能清单。

### 6.1 v1.0 包含功能

```
✅ 地图随机生成 + 20 波核心战斗
✅ 8 种防御塔 + 12 种怪物 + 4 BOSS
✅ 骰子机制（四池合并抽3 + 队友减益独立命中）
✅ 塔属性自动成长（8条成长曲线 + 硬上限）
✅ 抽卡系统（N/R/SR/SSR 四池 + 保底 + 轮换）
✅ 遗物系统 + Buff 管理
✅ 波次间商店（4次触发/局）+ 多路线分支
✅ 2-4 人联机协作 + 快速匹配（含 AI 填充）
✅ 房间系统 + 好友邀请 + 快捷消息
✅ 观战系统（10-30s 延迟，指令模式）
✅ 天赋树 + 成就系统 + 排行榜（含防刷）
✅ 结算评分 S/A/B/C/D
✅ 新手引导 + 功能渐进式解锁 + FTUE 漏斗
✅ 每日任务 + 每周挑战
✅ 后台管理系统 + 数据埋点体系
✅ 隐私合规 + 防沉迷 + 虚拟物品退款规则
✅ 无障碍基础支持：字体缩放 + 震动反馈
```

### 6.2 延期功能

| 功能 | 延期版本 | 延期理由 |
|------|:---:|------|
| 精彩回放 | v1.1 | 指令回放实现成本高，v1.0 仅保留战报图片分享 |
| 联机语音 | v1.1 | `wx.joinVoIPChat` API 兼容性问题 |
| 图鉴系统 | v1.1 | 纯收集系统，不影响核心玩法 |
| 色盲模式 | v1.1 | 配色方案替换，视排期决定 |
| 皮肤商城 | v1.2 | 依赖美术资源积累 |
| 通行证（战令） | v1.2 | 需运营数据支撑定价策略 |
| 激励视频 | v1.2 | 需对接广告平台 |

### 6.3 后台管理系统（独立项目）

> 📌 来自需求 §10：后台管理系统为 P0 级基础设施，但作为独立 Web 项目另行设计。

| 模块 | 核心功能 | 优先级 |
|------|---------|:---:|
| 用户管理 | openid查询、封禁/解封、数据修改 | P0 |
| 数据看板 | DAU/留存/付费/对局/社交指标 | P0 |
| 数值配置 | 塔/怪物/骰子池/抽卡池热更新（需审批流程） | P0 |
| 活动配置 | 限时活动/每日任务/节日活动管理 | P1 |
| 公告/推送 | 全服公告、微信服务通知 | P1 |
| 客服系统 | 工单管理、自动回复、申诉处理 | P1 |
| 日志审计 | 管理员操作日志、异常告警 | P0 |

---

*文档一完结。详见 [技术文档_02_战斗核心系统.md](./技术文档_02_战斗核心系统.md)*
